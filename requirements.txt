# ══════════════════════════════════════════════
# TEKNOFEST 2026 İKA — Jetson Orin Nano
# ══════════════════════════════════════════════
#
# DİKKAT: torch, torchvision, opencv-python base image'dan gelir.
# Bunları pip ile YENİDEN YÜKLEME — Jetson CUDA desteği bozulur.

# Ultralytics — torch bağımlılığını ATLA

# Ultralytics'in torch HARİÇ gerçek bağımlılıkları
matplotlib>=3.3.0
pandas>=1.1.4
seaborn>=0.11.0
Pillow>=7.1.2
pyyaml>=5.3.1
requests>=2.23.0
scipy>=1.4.1
tqdm>=4.64.0
psutil
py-cpuinfo
thop>=0.1.1

# STM32 UART
pyserial>=3.5

# OpenCV (cv2) ile uyumluluk için Numpy 1.x serisinde kalmalı!
numpy==1.26.4